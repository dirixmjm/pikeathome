Create variable control (rw, ro etc).
Create variable types (switch / float / int).
e.g. state == (setable) variable
Switchboard add hook.
Centralize the Logging structure more
Remove #ifdef's around logging, making it more runtime-dynamic
